@keyframes fade {
  100% { opacity: 0 }
}
@font-face {
  font-family: foo;
  src: url(src(./e/foo.tl)?#iefix) format('truetype');
}
@const {
  desktop: (min-width: 900px);
  fav-color: teal;
}

main, article { display: block; }
a {
  background: transparent;
  :active { outline: none }
}
svg:not(:root) { overflow: hidden; }
